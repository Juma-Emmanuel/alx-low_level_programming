#include "main.h"
#include <stdio.h>
#include <string.h>
/**
**_strlen - Entry point
*@s:- string input
*returns the length of a string.
* Return: Always 0.
*/
int _strlen(char *s)
{
        return (strlen(s));
}
