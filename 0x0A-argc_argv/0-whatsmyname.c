#include <stdio.h>
/**
 * main - Entry point
 *@argc: number of arguments
 *@argv: array of the string args
 *program that prints its name,
 * Return: Always 0 (Success)
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
